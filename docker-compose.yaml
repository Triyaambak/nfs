services:
  api:
    container_name: nfs-server
    build:
      context: ./api
      dockerfile: Dockerfile
      args:
        - API_PORT=${API_PORT}
    env_file:
      - ./.env
    ports:
      - ${API_PORT}:3001
    volumes:
      - nfs_volume:/app/nfs

volumes:
  nfs_volume:
